# ═══════════════════════════════════════════════
# PHANTOM TRADER — Full Stack Dependencies
# pip install -r requirements.txt
# ═══════════════════════════════════════════════

# === CORE (Required) ===
MetaTrader5>=5.0.45         # MT5 Python API (Windows only)
anthropic>=0.39.0           # Claude AI analyst
pandas>=2.1.0               # Data manipulation
numpy>=1.26.0               # Numerical computing
ta>=0.11.0                  # Technical indicators (RSI, MACD, BB, ADX, ATR)
python-dotenv>=1.0.0        # .env config loader

# === DASHBOARD ===
streamlit>=1.38.0           # Web GUI
plotly>=5.18.0              # Interactive charts

# === ASYNC ENGINE (Wolf v3.6) ===
aiohttp>=3.9.0              # Async HTTP for data_layer + intelligence_feed
feedparser>=6.0.0           # RSS parsing (SEC, DOJ, FDA, CNBC)

# === NLP & SENTIMENT ===
groq>=0.4.0                 # Groq NLP sniper (optional, needs GROQ_API_KEY)
textblob>=0.18.0            # Fallback sentiment analysis

# === NEWS & INTELLIGENCE ===
requests>=2.31.0            # HTTP client for news APIs

# === BACKTESTER ===
# cupy-cuda12x              # GPU Monte Carlo (optional — pip install separately)
#                            # Falls back to numpy if no CUDA GPU

# === UTILITIES ===
schedule>=1.2.0             # Task scheduling
